# lijiayang.net

Personal homepage of Jiayang Li — a single static `index.html` (no build step) served by GitHub Pages.

- `index.html` — the whole site: cover page + content page, all CSS/JS inlined
- `Files/` — avatar and favicon
- `CNAME` — custom domain
- `.nojekyll` — serve files as-is, skip Jekyll processing
